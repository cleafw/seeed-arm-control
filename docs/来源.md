# 来源

| 项 | 值 |
|---|---|
| 上游仓库 | https://github.com/Love4yzp/rebot-b601-102-record-demo |
| 上游 fork 自 | https://github.com/Welt-liu/rebot-b601-102-record-demo |
| 本机目录名 | `seeed-arm-control`（原 `rebot-b601-102-record`） |
| 基准 HEAD | `fee8dfd`（2026-05-13 +0800） |
| HEAD 说明 | `feat(deploy): port 1882, stable udev symlinks, reDM cgroup rules` |
| License | MIT；`backend/u2can/` 来自 [cmjang/DM_CAN](https://github.com/cmjang/DM_CAN)，MIT |

## 相对上游的改动

在 `fee8dfd` 之上增加校准、电机映射、free_move、回放过渡修复、双臂链路探活重连、现场 compose/udev 等。完整清单见 [`变更记录.md`](变更记录.md)。

文档：

- `SKILL.md` — Project / AI 入口
- `docs/功能说明.md` — 功能与部署
- `docs/变更记录.md` — 改动摘要
- 本文件

`deploy/docker-compose.yml` / `docker-compose.seeed.yml` 映射为 **`1882:8000`**。本机 `uvicorn` / Vite 仍用 8000。

## 部署约定（通用）

| 项 | 说明 |
|---|---|
| 访问 | `http://<host>:1882` |
| 环境变量 | 仓库根目录 `.env`（或自行改 compose 的 `env_file`） |
| 持久化 | 容器 `/app/recordings` ← 宿主机 `recordings/` |
