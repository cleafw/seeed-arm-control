# Placeholder package for the optional Voice module (ASR / rule NLU / TTS).
# Core remains the only motor writer — this package only calls Core HTTP.
#
# Run:  uv run uvicorn voice.app:app --host 0.0.0.0 --port 1883
# Docs: docs/语音部署说明.md
